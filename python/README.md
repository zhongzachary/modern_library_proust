This is for parsing raw text to more structured / formatted data.

To run it, cd into the `python` directory and run the `parse_raw_text.py` script (preferably use uv such as `uv run parse_raw_text.py`)
